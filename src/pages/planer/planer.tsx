import Features from '../../components/features/features';

const Planer = () => {

  return (
    <div className="forecast" style={{ margin: '40px 24px', width: '100vw' }}>
      <Features title={'Фильтр прогнозирования спроса товаров'} />
    </div>
  );
};

export default Planer;
